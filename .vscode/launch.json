{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Chrome",
      "request": "launch",
      "type": "pwa-chrome",
      "url": "http://localhost:8088",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "parallel_watch_serve",
      "runtimeArgs": ["--app=http://localhost:8088"]
    }
  ]
}
